# name: test/parquet/corrupted_bitwidth.test
# group: [parquet]

require parquet

statement error
SELECT * FROM read_parquet('{DATA_DIR}/parquet-testing/rle_malicious_bitwidth.parquet');
----
IO Error: The width (63) of the bitpacked data exceeds the maximum width (32) for the target type, the file might be corrupted.
