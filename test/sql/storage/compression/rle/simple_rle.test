# name: test/sql/storage/compression/test_rle.test
# description: Test storage with RLE
# group: [storage]

# load the DB from disk
load __TEST_DIR__/test_rle.db

statement ok
PRAGMA force_compression = 'rle'

# simple RLE with few values
statement ok
CREATE TABLE test (a INTEGER, b INTEGER);

statement ok
INSERT INTO test VALUES (11, 22), (11, 22), (11, 21), (NULL, NULL)

query II
SELECT SUM(a), SUM(b) FROM test
----
33	65

restart

query II
SELECT SUM(a), SUM(b) FROM test
----
33	65
