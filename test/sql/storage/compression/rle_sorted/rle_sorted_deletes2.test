# name: test/sql/storage/compression/rle_sorted/rle_sorted_deletes2.test
# description: Test deletes when sorting RLE - 2
# group: [rle_sorted]

# load the DB from disk
load __TEST_DIR__/test_store_deletes.db

statement ok
PRAGMA wal_autocheckpoint='1TB';

statement ok
CREATE TABLE integers AS SELECT * FROM range(11) tbl(i);

statement ok
DELETE FROM integers WHERE i<=8

statement ok
CHECKPOINT;

query III
SELECT min(i), max(i), count(i) from integers;
----
9	10	2
