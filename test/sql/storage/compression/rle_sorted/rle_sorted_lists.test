# name: test/sql/storage/compression/rle_sorted/rle_sorted_lists.test
# description: INSERT and sort lists when using RLESort - use STANDARD_VECTOR_SIZE=2
# group: [rle_sorted]

# load the DB from disk
load __TEST_DIR__/test_rle.db

statement ok
PRAGMA force_compression = 'rle'

statement ok
PRAGMA preserve_insertion_order = false

statement ok
PRAGMA force_row_group_replacement = true

statement ok
CREATE TABLE test (id INTEGER, l INTEGER[]);

statement ok
INSERT INTO test SELECT i, [1] FROM range(3) tbl(i)

query I
SELECT compression FROM pragma_storage_info('test') WHERE segment_type ILIKE 'INTEGER' LIMIT 1
----
RLE
