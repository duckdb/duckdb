# name: test/sql/storage/compression/rle_sorted/rle_sorted_deletes_morsel.test
# description: Test deletes correctly with statement directly after
# group: [rle_sorted]

load __TEST_DIR__/checkpoint_morsel_size.db

statement ok
CREATE TABLE integers AS SELECT * FROM range(2*100*8) tbl(i);

query III
SELECT min(i), max(i), count(i) from integers;
----
0	1599	1600

restart

statement ok
DELETE FROM integers WHERE i<=100

query III
SELECT min(i), max(i), count(i) from integers;
----
101	1599	1499

