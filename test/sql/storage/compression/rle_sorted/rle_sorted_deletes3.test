# name: test/sql/storage/compression/rle_sorted/rle_sorted_deletes3.test
# description: Test deletes when sorting RLE - 3
# group: [rle_sorted]

load __TEST_DIR__/checkpoint_morsel_size.db

statement ok
CREATE TABLE integers AS SELECT * FROM range(1024*100*8) tbl(i);

query III
SELECT min(i), max(i), count(i) from integers;
----
0	819199	819200

restart

statement ok
DELETE FROM integers WHERE i<=1000

query III
SELECT min(i), max(i), count(i) from integers;
----
1001	819199	818199

