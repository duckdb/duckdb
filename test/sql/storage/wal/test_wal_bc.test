# name: test/sql/storage/wal/test_wal_bc.test
# description: Test WAL backwards compatibility
# group: [wal]

# FIXME: changing both the block size (16KB) and the vector size (512) causes this test to fail with:
# FIXME: Exception in WAL playback during initial read: INTERNAL Error [...]
require block_size 262144

load data/storage/wal_test_092.db readonly

query II
SELECT COUNT(*), SUM(i) FROM integers
----
1000	499500
