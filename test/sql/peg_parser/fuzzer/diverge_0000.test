# name: fuzzer_issues/diverge_0000.test
# description: PEG should reject (currently accepts): Parser Error: syntax error at or near "EXISTS"
# group: [peg_parser]

require autocomplete

statement ok
CALL enable_peg_parser();

statement ok
DETACH IF EXISTS my_table;

statement ok
CALL disable_peg_parser();

statement ok
DETACH IF EXISTS my_table
