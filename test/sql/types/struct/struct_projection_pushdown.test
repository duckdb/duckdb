# name: test/sql/types/struct/struct_projection_pushdown.test
# description: Test struct projection pushdown
# group: [struct]

statement ok
PRAGMA enable_verification

statement ok
CREATE TABLE test_structs(s STRUCT(a integer, b bool));

statement ok
INSERT INTO test_structs VALUES ({'a': 42, 'b': true}), (NULL), ({'a': 84, 'b': NULL}), ({'a': NULL, 'b': false});

query I
SELECT s.b FROM test_structs
----
true
NULL
NULL
false
