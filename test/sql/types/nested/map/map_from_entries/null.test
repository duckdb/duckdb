# name: test/sql/types/nested/map/null.test
# description: Show expected behavior when the provided list of entries is NULL
# group: [map]

statement ok
PRAGMA enable_verification

# Provided argument is NULL
statement error
SELECT MAP_FROM_ENTRIES(NULL);
