# name: test/sql/copy/csv/tsv_copy.test
# description: Test TSV Copy round-trip
# group: [csv]

statement ok
PRAGMA enable_verification

statement ok
CREATE TABLE people(id INTEGER, name VARCHAR);

statement ok
INSERT INTO people VALUES (1, 'Mark'), (2, 'Hannes');

statement ok
COPY people TO '{TEMP_DIR}/test.tsv' WITH (DELIMITER '\t');

query II
SELECT * FROM '{TEMP_DIR}/test.tsv'
----
1	Mark
2	Hannes

query II
SELECT * FROM read_csv('{TEMP_DIR}/test.tsv', sep='\t', columns={'id': 'INTEGER', 'name': 'VARCHAR'})
----
1	Mark
2	Hannes
