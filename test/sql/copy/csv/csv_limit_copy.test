# name: test/sql/copy/csv/csv_limit_copy.test
# description: CSV limit copy
# group: [csv]

statement ok
CREATE TABLE integers AS FROM range(1000000) t(i);

statement ok
COPY (FROM integers LIMIT 30000) TO '{TEST_DIR}/limit_copy.csv'

query I
SELECT COUNT(*) FROM '{TEST_DIR}/limit_copy.csv'
----
30000
