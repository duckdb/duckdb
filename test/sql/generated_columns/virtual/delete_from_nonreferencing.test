# name: test/sql/generated_columns/virtual/delete_from_nonreferencing.test
# group: [virtual]

statement ok
pragma enable_verification

# Create a table containing nothing more than 1 generated column
# this generated column does not reference another column
statement ok
CREATE TEMP TABLE t0(c0 AS (1));

# Trying to delete from a (physically) empty table causes an error
statement error
DELETE FROM t0;
