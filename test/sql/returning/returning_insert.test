# name: test/sql/returning/returning_insert.test
# description: Test returning with top level INSERT statement
# group: [returning]

statement ok
CREATE TABLE table1 (a INTEGER, b INTEGER, c INTEGER);

# returning 1 column
query I
INSERT INTO table1 VALUES (1, 2, 3) RETURNING a;
----
1   2   3

# returning all columns
query I
INSERT INTO table1 VALUES (1, 2, 3) RETURNING *;
----
1   2   3

# returning constant expression
query I
INSERT INTO table1 VALUES (1, 2, 3) RETURNING 100, "hello duckdb";
----
100 "hello duckdb"

# returning using a column alias
query I
INSERT INTO table1 VALUES (1, 2, 3) RETURNING 100 AS aliascol;
----
100

# returning the result of inserting multiple columns
query I
INSERT INTO table1 VALUES (1, 2, 3), (4, 5, 6), (7, 8, 9) RETURNING *;
----
1   2   3
4   5   6
7   8   9

# returning expression not in select statement
query I
INSERT INTO table1 VALUES (10, 20, 30), (40, 50, 60), (70, 80, 90) RETURNING a, b, c, c NOT BETWEEN 35 AND 65;
----
10  20  30  1
40  50  60  0
70  80  90  1


# insert only a subset of columns, returning * = don't know what this would be yet
# insert only a subset of columns, return just the subset of columns
# insert only a subset of columns, return other columns, should be error
# insert into table using select, return all columns
# insert into a table, return the columns in a different order
