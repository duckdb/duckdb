WITH RECURSIVE tbl(a,b,c) USING KEY (a, b ; list(c) FILTER (c IS NOT NULL)) AS (
         SELECT 1, 1, NULL
             UNION
         SELECT tbl.a, tbl.b - 1, a.b
         FROM tbl, recurring.tbl AS a
         WHERE tbl.b > 0)
     SELECT *
     FROM tbl;
----
1	1	2.5	[5.0, 4.5, 4.0, 3.5, 3.0]

