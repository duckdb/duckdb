# name: test/sql/constraints/foreignkey/fk_<ISSUE>.test
# description: Issue #20530: Unqualified foreign key schema resolution
# group: [foreignkey]

statement ok
CREATE SCHEMA freddy;

statement ok
SET schema = freddy;

statement ok
CREATE TABLE zippy(
    id INTEGER PRIMARY KEY
);

statement ok
CREATE TABLE george(
    zippy_id INTEGER,
    FOREIGN KEY (zippy_id) REFERENCES zippy(id)
);

statement ok
INSERT INTO zippy VALUES (1);

statement ok
INSERT INTO george VALUES (1);
