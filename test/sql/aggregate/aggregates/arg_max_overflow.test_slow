# name: test/sql/aggregate/aggregates/arg_max_overflow.test_slow
# group: [aggregates]

mode skip Test is 20 seconds on a release build

query I
SELECT arg_max(a, b, 1) FROM (VALUES
	(repeat('a', 1500000000), 1),
	(repeat('z', 2200000000), 2)
) t(a, b);
----
Invalid Input Error: Resulting string/blob too large!

mode unskip
