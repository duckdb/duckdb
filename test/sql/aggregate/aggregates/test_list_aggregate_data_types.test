# name: test/sql/aggregate/aggregates/test_list_aggregate_data_types.test
# description: Test the list aggregate function for all data types
# group: [aggregates]

# TODO: write these tests properly

statement ok
CREATE TABLE ints (g INTEGER, i INTEGER)

statement ok
INSERT INTO ints VALUES (1, 2), (1, 3), (2, 4)

query I
SELECT LIST(i) FROM ints GROUP BY g ORDER BY g
----
[2, 3]
[4]

statement ok
CREATE TABLE big_ints (g BIGINT, i BIGINT)

statement ok
INSERT INTO big_ints VALUES (1, 2), (1, 3), (2, 4)

query I
SELECT LIST(i) FROM big_ints GROUP BY g ORDER BY g
----
[2, 3]
[4]
