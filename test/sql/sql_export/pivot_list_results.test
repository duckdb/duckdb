# name: test/sql/sql_export/pivot_list_results.test
# description: List PIVOT results, errors and list ordering settings
# group: [sql_export]

statement ok
SET debug_verify_sql_export='strict';

statement ok
SET threads=1;

statement ok
SET pivot_filter_threshold=0;

statement ok
SET pivot_filter_threshold=0;

query TTTTTTT
SELECT * FROM (VALUES ('g','a',10),('g','a',NULL),('g','b',20),('h','a',NULL)) t(g,k,v) PIVOT (sum(v) AS s,count(v) AS c FOR k IN ('a','b','z')) ORDER BY g;
----
g	10	1	20	1	NULL	0
h	NULL	0	NULL	0	NULL	0

statement ok
SET pivot_filter_threshold=3;

query TTTTTTT
SELECT * FROM (VALUES ('g','a',10),('g','a',NULL),('g','b',20),('h','a',NULL)) t(g,k,v) PIVOT (sum(v) AS s,count(v) AS c FOR k IN ('a','b','z')) ORDER BY g;
----
g	10	1	20	1	NULL	0
h	NULL	0	NULL	0	NULL	0

statement ok
SET pivot_filter_threshold=0;

query TTT
FROM (VALUES ('a',1),('b',2)) t(k,v) PIVOT (sum(v) FOR k IN ('a','b','z'));
----
1	2	NULL

statement ok
SET pivot_filter_threshold=3;

query TTT
FROM (VALUES ('a',1),('b',2)) t(k,v) PIVOT (sum(v) FOR k IN ('a','b','z'));
----
1	2	NULL

statement ok
SET pivot_filter_threshold=0;

query TT
EXPLAIN SELECT a_ok FROM (VALUES ('a',1),('b',2)) t(k,v) PIVOT (sum(v) AS ok,sum(CASE WHEN v=2 THEN error('pivot export error')::INTEGER ELSE v END) AS effect FOR k IN ('a','b','z'));
----
physical_plan	<REGEX>:.*Pivot.*

statement ok
SET debug_verify_serializer=false;

statement error
SELECT a_ok FROM (VALUES ('a',1),('b',2)) t(k,v) PIVOT (sum(v) AS ok,sum(CASE WHEN v=2 THEN error('pivot export error')::INTEGER ELSE v END) AS effect FOR k IN ('a','b','z'));
----
pivot export error

statement ok
SET debug_verify_serializer=true;

statement error
SELECT a_ok FROM (VALUES ('a',1),('b',2)) t(k,v) PIVOT (sum(v) AS ok,sum(CASE WHEN v=2 THEN error('pivot export error')::INTEGER ELSE v END) AS effect FOR k IN ('a','b','z'));
----
pivot export error

statement ok
SET default_collation='nocase';

statement ok
SET debug_verify_serializer=false;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_list_sort_nocase_false
SELECT list_sort(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_list_sort_nocase_false
SELECT list_sort(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET default_collation='nocase';

statement ok
SET debug_verify_serializer=true;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_list_sort_nocase_true
SELECT list_sort(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_list_sort_nocase_true
SELECT list_sort(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET default_collation='nocase';

statement ok
SET debug_verify_serializer=false;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_array_sort_nocase_false
SELECT array_sort(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_array_sort_nocase_false
SELECT array_sort(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET default_collation='nocase';

statement ok
SET debug_verify_serializer=true;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_array_sort_nocase_true
SELECT array_sort(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_array_sort_nocase_true
SELECT array_sort(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET default_collation='nocase';

statement ok
SET debug_verify_serializer=false;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_list_grade_up_nocase_false
SELECT list_grade_up(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_list_grade_up_nocase_false
SELECT list_grade_up(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET default_collation='nocase';

statement ok
SET debug_verify_serializer=true;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_list_grade_up_nocase_true
SELECT list_grade_up(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_list_grade_up_nocase_true
SELECT list_grade_up(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET default_collation='nocase';

statement ok
SET debug_verify_serializer=false;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_array_grade_up_nocase_false
SELECT array_grade_up(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_array_grade_up_nocase_false
SELECT array_grade_up(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET default_collation='nocase';

statement ok
SET debug_verify_serializer=true;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_array_grade_up_nocase_true
SELECT array_grade_up(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_array_grade_up_nocase_true
SELECT array_grade_up(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET default_collation='nocase';

statement ok
SET debug_verify_serializer=false;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_grade_up_nocase_false
SELECT grade_up(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_grade_up_nocase_false
SELECT grade_up(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET default_collation='nocase';

statement ok
SET debug_verify_serializer=true;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_grade_up_nocase_true
SELECT grade_up(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_grade_up_nocase_true
SELECT grade_up(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET default_collation='nocase';

statement ok
SET debug_verify_serializer=false;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_list_reverse_sort_nocase_false
SELECT list_reverse_sort(x) FROM (VALUES (['B','b']::VARCHAR[]))t(x);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_list_reverse_sort_nocase_false
SELECT list_reverse_sort(x) FROM (VALUES (['B','b']::VARCHAR[]))t(x);

statement ok
SET default_collation='nocase';

statement ok
SET debug_verify_serializer=true;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_list_reverse_sort_nocase_true
SELECT list_reverse_sort(x) FROM (VALUES (['B','b']::VARCHAR[]))t(x);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_list_reverse_sort_nocase_true
SELECT list_reverse_sort(x) FROM (VALUES (['B','b']::VARCHAR[]))t(x);

statement ok
SET default_collation='nocase';

statement ok
SET debug_verify_serializer=false;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_array_reverse_sort_nocase_false
SELECT array_reverse_sort(x) FROM (VALUES (['B','b']::VARCHAR[]))t(x);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_array_reverse_sort_nocase_false
SELECT array_reverse_sort(x) FROM (VALUES (['B','b']::VARCHAR[]))t(x);

statement ok
SET default_collation='nocase';

statement ok
SET debug_verify_serializer=true;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_array_reverse_sort_nocase_true
SELECT array_reverse_sort(x) FROM (VALUES (['B','b']::VARCHAR[]))t(x);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_array_reverse_sort_nocase_true
SELECT array_reverse_sort(x) FROM (VALUES (['B','b']::VARCHAR[]))t(x);

statement ok
SET default_collation='nocase';

statement ok
SET debug_verify_serializer=false;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_list_sort_list_nocase_false
SELECT list_sort(list(x)) FROM (VALUES ('b',0),('B',1))t(x,r);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_list_sort_list_nocase_false
SELECT list_sort(list(x)) FROM (VALUES ('b',0),('B',1))t(x,r);

statement ok
SET default_collation='nocase';

statement ok
SET debug_verify_serializer=true;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_list_sort_list_nocase_true
SELECT list_sort(list(x)) FROM (VALUES ('b',0),('B',1))t(x,r);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_list_sort_list_nocase_true
SELECT list_sort(list(x)) FROM (VALUES ('b',0),('B',1))t(x,r);

statement ok
SET default_collation='';

statement ok
SET debug_verify_serializer=false;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_list_sort_binary_false
SELECT list_sort(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_list_sort_binary_false
SELECT list_sort(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET default_collation='';

statement ok
SET debug_verify_serializer=true;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_list_sort_binary_true
SELECT list_sort(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_list_sort_binary_true
SELECT list_sort(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET default_collation='';

statement ok
SET debug_verify_serializer=false;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_array_sort_binary_false
SELECT array_sort(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_array_sort_binary_false
SELECT array_sort(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET default_collation='';

statement ok
SET debug_verify_serializer=true;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_array_sort_binary_true
SELECT array_sort(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_array_sort_binary_true
SELECT array_sort(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET default_collation='';

statement ok
SET debug_verify_serializer=false;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_list_grade_up_binary_false
SELECT list_grade_up(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_list_grade_up_binary_false
SELECT list_grade_up(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET default_collation='';

statement ok
SET debug_verify_serializer=true;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_list_grade_up_binary_true
SELECT list_grade_up(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_list_grade_up_binary_true
SELECT list_grade_up(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET default_collation='';

statement ok
SET debug_verify_serializer=false;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_array_grade_up_binary_false
SELECT array_grade_up(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_array_grade_up_binary_false
SELECT array_grade_up(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET default_collation='';

statement ok
SET debug_verify_serializer=true;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_array_grade_up_binary_true
SELECT array_grade_up(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_array_grade_up_binary_true
SELECT array_grade_up(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET default_collation='';

statement ok
SET debug_verify_serializer=false;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_grade_up_binary_false
SELECT grade_up(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_grade_up_binary_false
SELECT grade_up(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET default_collation='';

statement ok
SET debug_verify_serializer=true;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_grade_up_binary_true
SELECT grade_up(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_grade_up_binary_true
SELECT grade_up(x) FROM (VALUES (['b','B']::VARCHAR[]))t(x);

statement ok
SET default_collation='';

statement ok
SET debug_verify_serializer=false;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_list_reverse_sort_binary_false
SELECT list_reverse_sort(x) FROM (VALUES (['B','b']::VARCHAR[]))t(x);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_list_reverse_sort_binary_false
SELECT list_reverse_sort(x) FROM (VALUES (['B','b']::VARCHAR[]))t(x);

statement ok
SET default_collation='';

statement ok
SET debug_verify_serializer=true;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_list_reverse_sort_binary_true
SELECT list_reverse_sort(x) FROM (VALUES (['B','b']::VARCHAR[]))t(x);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_list_reverse_sort_binary_true
SELECT list_reverse_sort(x) FROM (VALUES (['B','b']::VARCHAR[]))t(x);

statement ok
SET default_collation='';

statement ok
SET debug_verify_serializer=false;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_array_reverse_sort_binary_false
SELECT array_reverse_sort(x) FROM (VALUES (['B','b']::VARCHAR[]))t(x);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_array_reverse_sort_binary_false
SELECT array_reverse_sort(x) FROM (VALUES (['B','b']::VARCHAR[]))t(x);

statement ok
SET default_collation='';

statement ok
SET debug_verify_serializer=true;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_array_reverse_sort_binary_true
SELECT array_reverse_sort(x) FROM (VALUES (['B','b']::VARCHAR[]))t(x);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_array_reverse_sort_binary_true
SELECT array_reverse_sort(x) FROM (VALUES (['B','b']::VARCHAR[]))t(x);

statement ok
SET default_collation='';

statement ok
SET debug_verify_serializer=false;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_list_sort_list_binary_false
SELECT list_sort(list(x)) FROM (VALUES ('b',0),('B',1))t(x,r);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_list_sort_list_binary_false
SELECT list_sort(list(x)) FROM (VALUES ('b',0),('B',1))t(x,r);

statement ok
SET default_collation='';

statement ok
SET debug_verify_serializer=true;

statement ok
SET debug_verify_sql_export='off';

query T nosort list_list_sort_list_binary_true
SELECT list_sort(list(x)) FROM (VALUES ('b',0),('B',1))t(x,r);

statement ok
SET debug_verify_sql_export='strict';

query T nosort list_list_sort_list_binary_true
SELECT list_sort(list(x)) FROM (VALUES ('b',0),('B',1))t(x,r);

statement ok
SET pivot_limit=3;

statement error
FROM (VALUES ('a',1),('b',2)) t(k,v) PIVOT (sum(v) FOR k IN ('a','b','z'));
----
<REGEX>:Binder Error: Pivot column limit of 3 exceeded.*

statement error
EXPLAIN (SQL) FROM (VALUES ('a',1),('b',2)) t(k,v) PIVOT (sum(v) FOR k IN ('a','b','z'));
----
<REGEX>:Binder Error: Pivot column limit of 3 exceeded.*

statement ok
RESET pivot_limit;
