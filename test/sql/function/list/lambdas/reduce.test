# name: test/sql/function/list/lambdas/reduce.test
# description: Test list_reduce function
# group: [lambdas]

# FIXME: list_reduce is not yet implemented
# NOTE: some of these tests are directly taken from the Presto Array Function examples

mode skip

query I
SELECT list_reduce([5, 20, 50], (s, x) -> s + x)
----
75

query I
SELECT list_reduce([5, 20, NULL, 50], (s, x) -> s + x)
----
NULL

query I
SELECT list_reduce([5, 20, NULL, 50], (s, x) -> s + COALESCE(x, 0))
----
75

query I
select list_reduce(i, (x, y) -> x + y) from (values (list_value(1, 2, 3))) tbl(i);
----
6

mode unskip