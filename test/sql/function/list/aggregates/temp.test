# name: test/sql/function/list/aggregates/temp.test
# description: Test the list_min, list_max aggregate functions
# group: [list]

statement ok
CREATE TABLE integers(i INTEGER[]);

statement ok
INSERT INTO integers VALUES ([1, 2, 3]), ([6, 3, 2, 5]), ([]), ([NULL]), (NULL), ([1, NULL, 2, 3]);

query I
SELECT list_avg(i) FROM integers;
----
2
4
NULL
NULL
NULL
2

query II
SELECT list_avg(i), list_avg(i) FROM integers;
----
2	2
4	4
NULL	NULL
NULL	NULL
NULL	NULL
2	2

query I
SELECT list_avg([1, 1, 1, 1]);
----
1

query I
SELECT list_avg([1, 1, 1, 1]) FROM integers;
----
1
1
1
1
1