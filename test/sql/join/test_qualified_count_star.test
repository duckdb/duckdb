# name: test/sql/join/test_qualified_count_star.test
# description: Test qualified count star
# group: [join]

statement ok
CREATE TABLE a(id int4);

statement ok
CREATE TABLE b(id int4);

statement ok
INSERT INTO a (id) VALUES (1), (2), (3);

statement ok
INSERT INTO b (id) VALUES (2), (3), (3);

query IIIII
SELECT a.id, COUNT(*), COUNT(a.*) AS a_start, COUNT(b.*) AS b_star, COUNT(b.id) AS b_id FROM a LEFT JOIN b ON a.id = b.id GROUP BY a.id;
----
1	1	1	0	0
2	1	1	1	1
3	2	2	2	2
