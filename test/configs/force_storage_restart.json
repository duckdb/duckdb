{
  "description": "Equivalent to CI --force-storage: persistent DuckDB database AND verify_fetch_row",
  "initial_db": "{TEST_DIR}/{BASE_TEST_NAME}/memory.db",
  "force_restart": "true",
  "skip_compiled": "true"
}
