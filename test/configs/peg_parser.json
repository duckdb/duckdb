{
  "description": "Test PEG Parser + transformer with fallback mode",
  "skip_compiled": "true",
  "on_init": "set allow_parser_override_extension=fallback;",
  "statically_loaded_extensions": ["core_functions", "autocomplete"],
  "skip_tests": [
    {
      "reason": "These tests test parser override extension behavior which is incompatible with PEG parser being more permissive than the Postgres parser",
      "paths": [
        "test/extension/loadable_parser_override.test",
        "test/extension/consistent_semicolon_extension_parse.test"
      ]
    }
  ]
}
