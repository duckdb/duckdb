# Normal SQL tests. Prefix exclusion patterns with !.
test/sql/**/*.test
# Rebinding adds transaction log events.
!test/sql/logging/logging.test
# Rebinding adds file operation log events.
!test/sql/logging/logging_csv.test
# Rebinding adds typed file operation log events.
!test/sql/logging/logging_types.test
# Replanning can change the optimizer statistics returned by stats().
!test/sql/types/geo/geometry_from_wkb_stats.test
# Replanning can change the optimizer statistics returned by stats().
!test/sql/types/geo/geometry_vertex_extract.test
# Native join reordering changes whether an empty-input cast error is evaluated.
!test/sql/join/natural/natural_join.test
