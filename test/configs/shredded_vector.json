{
	"description": "Run with verify-vector 'shredded_vector'.",
	"on_init": "PRAGMA debug_verify_vector = 'shredded_vector';",
	"skip_compiled": "true",
	"skip_tests": [
		{
			"reason": "These tests need one shredded and one unshredded row group in the same table, which forcing every vector to be shredded takes away",
			"paths": [
				"test/sql/storage/types/variant/variant_object_key_order_shredded.test",
				"test/sql/storage/types/variant/variant_object_key_order_shredded_json.test"
			]
		}
	]
}
