{
  "description": "Run with debug_force_external=true.",
  "on_init": "SET debug_force_external=true;",
  "skip_compiled": "true",
  "skip_tests": [
    {
      "reason": "Explicit memory limit is exceeded with force external",
      "paths": [
        "test/sql/index/art/storage/test_art_mem_limit.test"
      ]
    }
  ]
}
