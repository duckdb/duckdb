# name: test/extension/test_loadable_optimizer.test
# description: Enable Test alias for point.
# group: [extension]

require skip_reload

statement skip_test_on_error
SELECT assert_true(platform NOT LIKE '%_mingw', 'requires a non-mingw platform') FROM pragma_platform()

mode skip instable

# FIXME: not going to do this now
statement ok
LOAD '{BUILD_DIRECTORY}/test/extension/loadable_extension_optimizer_demo.duckdb_extension';

statement ok
select 42
