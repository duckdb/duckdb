# name: test/extension/load_extension.test
# description: Try loading a shared object with an extension
# group: [extension]

statement error
LOAD 'asdf';

statement error
LOAD 'Makefile';

statement error
LOAD NULL;

statement ok
LOAD 'build/debug/test/extension/loadable_extension_demo.so';

query I
SELECT hello('World');
----
Hello, World
