# name: test/temp.test
# group: [test]

statement ok
CREATE TABLE varcharlists AS SELECT [i, i+1, i+2]::VARCHAR col FROM range(0, 10000) tbl(i);

query I
SELECT MAX(CAST(col AS INT[])) FROM varcharlists;
----
[9999, 10000, 10001]