# name: test/fuzzer/duckfuzz/arrow_scan_subquery_nullptr.test
# description: Fuzzyduck issue #1212
# group: [duckfuzz]

statement error
SELECT * FROM (SELECT * FROM arrow_scan(NULL, NULL, NULL));
----
No function matches the given name and argument types

statement error
SELECT * FROM (SELECT * FROM arrow_scan());
----
Binder Error: arrow_scan requires an ArrowScanFactory bind input
