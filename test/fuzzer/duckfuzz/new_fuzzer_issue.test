# name: test/fuzzer/duckfuzz/new_fuzzer_issue.test
# description: Type mismatch in map concat
# group: [duckfuzz]

require tpch

statement ok
call dbgen(sf=0.1);

statement ok
SELECT mean(c1) FROM test_vector_types(CAST(NULL AS TIMESTAMP WITH TIME ZONE)) AS test_vector_types(c1);