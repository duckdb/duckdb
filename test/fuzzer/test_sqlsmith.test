# name: test/fuzzer/test_sqlsmith.test
# group: [fuzzer]

require tpch

require sqlsmith

statement ok
create table all_types as select * from test_all_types();

statement ok
call sqlsmith(max_queries=1000, seed=42, verbose_output=1, log='sqlsmith.log', complete_log='sqlsmith.complete.log');
