# name: ${FILE_PATH}
# description: ${DESCRIPTION}
# group: [ingestion]

name Q${table_name}
group ingestion
subgroup tpcds

require tpcds

require parquet

cache tpcds_sf1_parquet.duckdb

load benchmark/ingestion/tpcds/load.sql

run
create table ${table_name}_ingested as select * from read_parquet('${BENCHMARK_DIR}/tpcds_parquet/${table_name}.parquet');

cleanup
drop table ${table_name}_ingested;