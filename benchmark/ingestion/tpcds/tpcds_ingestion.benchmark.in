# name: ${FILE_PATH}
# description: ${DESCRIPTION}
# group: [ingestion]

name Q${table_name}
group ingestion
subgroup tpch

require tpcds

cache tpcds_sf5.duckdb

load benchmark/ingestion/tpcds/load.sql

run
create table ${table_name}_ingested as select * from ${table_name};

cleanup
DROP TABLE ${table_name}_ingested;