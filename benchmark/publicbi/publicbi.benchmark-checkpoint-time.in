# name: ${FILE_PATH}
# description: ${DESCRIPTION}
# group: [publicbi-compression]

name PUBLICBI_${SUBSET}_${QUERY_NUMBER}
group publicbi-compression

load benchmark/publicbi/data/${SUBSET}/load.sql

run
PRAGMA force_compression_sorting=${SORTINGBOOL};
CHECKPOINT;

cleanup benchmark/publicbi/data/${SUBSET}/drop.sql