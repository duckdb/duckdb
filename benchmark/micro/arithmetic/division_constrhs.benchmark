# name: benchmark/micro/arithmetic/multiplications.benchmark
# description: Integer multiplications between 10000000 values
# group: [arithmetic]

name Integer Multiplication
group micro

load
CREATE TABLE integers AS SELECT ((i * 53) % 100)::INTEGER AS i FROM range(0, 1000000000) tbl(i);

run
SELECT MIN(i//100) FROM integers

result I
0
