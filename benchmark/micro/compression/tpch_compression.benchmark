# name: benchmark/micro/compression/tpch_compression.benchmark
# description: Storing tpc-h sf1 database with default compression
# group: [compression]

name TPC-H SF1 Write with default compression
group compression
storage persistent
require tpch
require_reinit

load
PRAGMA force_compression='none';

run
call dbgen(sf=1);
checkpoint;

