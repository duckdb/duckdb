# name: benchmark/micro/map/map_insert.benchmark
# description: insert speed of map
# group: [map]

name Insert
group micro
subgroup map

load
CREATE TABLE mapvalues(col MAP(int, int));

run
INSERT INTO mapvalues SELECT MAP([i, i+1, i+2],[i+1, i+2, i+3]) col FROM range(0, 1000000) tbl(i);

