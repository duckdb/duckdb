# name: benchmark/micro/logger/disabled/logging_disabled_thread_local.benchmark
# description: Benchmarking the Thread Local logger
# group: [disabled]

name FileOpener NopLogger
group logger

# Note: this will call the Logger, but logging is disabled so this will call the NopLogger 100000000 times in a tight inner loop
run
SELECT sum(value) FROM test_logging(50000000, enable_logging=true, log_message='hello world', logger='thread_local');

result I
1249999975000000
