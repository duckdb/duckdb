# name: benchmark/realnest/12_map_two_list_values.benchmark
# description: Map two list values
# group: [realnest]

name map_two_list_values
group real_nest

require json

require httpfs

cache real_nest.duckdb

load benchmark/realnest/load.sql

run
SELECT map([gh_issue.issue.user.login],[gh_pull.pull_request.body]) 
FROM gh_pull, gh_issue WHERE gh_issue.issue.user = gh_pull.pull_request.head.repo.owner 
GROUP BY ALL;