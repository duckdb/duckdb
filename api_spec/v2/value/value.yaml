module: value
#
# Value construction, reading, NULL, composition, descent, and cast.
#
# A value carries a logical type plus a typed payload, stored as:
#   - primitives: an inline union (bool, the integer widths, float/double,
#     hugeint, interval, and every date/time/timestamp variant).
#   - varchar/blob/bit/bignum/geometry: a managed byte string.
#   - type: a managed byte string holding the serialized logical type, which
#     value_create_type_with_context / _with_connection wrap and value_get_type
#     unwraps.
#   - composites (list/struct/tuple/map/array/union): a vector of child values,
#     built via value_create (LIST/ARRAY/STRUCT/TUPLE/MAP) or via value_cast
#     (UNION from a member value, ENUM from a VARCHAR), and read via
#     value_get_child_count / value_get_child.
#
# Leaf payloads cross the boundary through the per-kind typed pairs:
# value_create_<kind>_with_context / _with_connection builds one, and
# value_get_<kind> reads one back. The constructors take a context or a
# connection because a value's construction is scoped to a catalog; the getters
# need no scope. A getter reads its own type id directly and converts anything
# else through the default cast set, on a copy, so a read never alters the
# value it reads. Three do not convert: value_get_varchar and value_get_blob
# hand back a pointer into the value, which a converted copy could not outlive,
# and value_get_decimal reports storage rather than a converted number.
#
# The typed set covers the built-ins whose payload is a plain C scalar or byte
# string. Every other kind — ENUM, UNION, VARIANT, extension types — is reached
# through value_cast, which is also how DuckDB itself constructs them: build
# the nearest typed value, usually a VARCHAR, and cast it. VARIANT casts both
# ways, so a boxed cell is read by casting it to the type it carries, or to
# VARCHAR, which always works, and built by casting into it; there is no
# unwrapping getter. BIGNUM reads back through value_get_blob, whose bytes are
# opaque storage — negatives are stored bit-inverted behind a header — and the
# vector module's bignum_encode / bignum_decode translate between those bytes
# and a magnitude plus a sign flag.
#
# All constructors copy their input. Getters return either owned outputs, which
# the caller destroys, or borrowed pointers, valid until the value handle is
# destroyed; each function documents which.
#
# NULL handling: the payload readers (value_get_<kind>, value_get_type) return
# ERROR_INPUT_INVALID for a NULL value, which has no typed payload to hand
# back. Check with value_is_null first, or use value_get_logical_type or
# value_to_string, both of which accept NULL values.

functions:
  # -----------------------------------------------------------------------
  # Lifecycle + NULL
  # -----------------------------------------------------------------------
  value_destroy:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Destroys a value handle.

      Null-safe: passing nullptr or a slot already set to nullptr is a no-op.
      On success the slot is set to nullptr.
    role: destructor
    belongs_to: value
    parameters:
      value:
        type: value
        indirection: 1
        description: The value to destroy.
    return_type: ERROR
  value_create_null:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a NULL value of the given logical type.

      The logical type is borrowed and copied into the value, so the caller can
      destroy it independently.
    role: constructor
    belongs_to: value
    parameters:
      type:
        type: logical_type
        description: The borrowed logical type to attach to the NULL value.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new NULL value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR
  # --------------------------------------------------------------------------------------------------------------------
  # Accessors
  # --------------------------------------------------------------------------------------------------------------------
  value_get_bool:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Returns the value as a BOOLEAN.

      A value of a different type is converted through the default cast set, on
      a copy, so reading never alters the value. An unsupported conversion
      returns ERROR_INPUT_INVALID, as does a NULL value.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out:
        type: bool
        indirection: 1
        kind: OUT
        description: Receives the payload as a bool.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_get_utinyint:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Returns the value as a UTINYINT.

      A value of a different type is converted through the default cast set, on
      a copy, so reading never alters the value. An unsupported conversion
      returns ERROR_INPUT_INVALID, as does a NULL value.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out:
        type: u8
        indirection: 1
        kind: OUT
        description: Receives the payload as a uint8_t.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_get_usmallint:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Returns the value as a USMALLINT.

      A value of a different type is converted through the default cast set, on
      a copy, so reading never alters the value. An unsupported conversion
      returns ERROR_INPUT_INVALID, as does a NULL value.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out:
        type: u16
        indirection: 1
        kind: OUT
        description: Receives the payload as a uint16_t.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_get_uint:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Returns the value as a UINTEGER.

      A value of a different type is converted through the default cast set, on
      a copy, so reading never alters the value. An unsupported conversion
      returns ERROR_INPUT_INVALID, as does a NULL value.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out:
        type: u32
        indirection: 1
        kind: OUT
        description: Receives the payload as a uint32_t.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_get_ubigint:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Returns the value as a UBIGINT.

      A value of a different type is converted through the default cast set, on
      a copy, so reading never alters the value. An unsupported conversion
      returns ERROR_INPUT_INVALID, as does a NULL value.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out:
        type: u64
        indirection: 1
        kind: OUT
        description: Receives the payload as a uint64_t.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_get_uhugeint:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Returns the value as a UHUGEINT.

      A value of a different type is converted through the default cast set, on
      a copy, so reading never alters the value. An unsupported conversion
      returns ERROR_INPUT_INVALID, as does a NULL value.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out:
        type: uhugeint_t
        indirection: 1
        kind: OUT
        description: Receives the payload as a uint128_t.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_get_tinyint:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Returns the value as a TINYINT.

      A value of a different type is converted through the default cast set, on
      a copy, so reading never alters the value. An unsupported conversion
      returns ERROR_INPUT_INVALID, as does a NULL value.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out:
        type: i8
        indirection: 1
        kind: OUT
        description: Receives the payload as an int8_t.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_get_smallint:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Returns the value as a SMALLINT.

      A value of a different type is converted through the default cast set, on
      a copy, so reading never alters the value. An unsupported conversion
      returns ERROR_INPUT_INVALID, as does a NULL value.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out:
        type: i16
        indirection: 1
        kind: OUT
        description: Receives the payload as an int16_t.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_get_int:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Returns the value as an INTEGER.

      A value of a different type is converted through the default cast set, on
      a copy, so reading never alters the value. An unsupported conversion
      returns ERROR_INPUT_INVALID, as does a NULL value.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out:
        type: i32
        indirection: 1
        kind: OUT
        description: Receives the payload as an int32_t.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_get_bigint:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Returns the value as a BIGINT.

      A value of a different type is converted through the default cast set, on
      a copy, so reading never alters the value. An unsupported conversion
      returns ERROR_INPUT_INVALID, as does a NULL value.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out:
        type: i64
        indirection: 1
        kind: OUT
        description: Receives the payload as an int64_t.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_get_hugeint:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Returns the value as a HUGEINT.

      A value of a different type is converted through the default cast set, on
      a copy, so reading never alters the value. An unsupported conversion
      returns ERROR_INPUT_INVALID, as does a NULL value.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out:
        type: hugeint_t
        indirection: 1
        kind: OUT
        description: Receives the payload as an int128_t.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_get_varchar:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Returns the value as a VARCHAR.

      The string is borrowed from the value and stays valid until the value is
      destroyed. That borrow is why this getter does not convert: a converted
      copy would not outlive the call. A value of any other type id, and a NULL
      value, return ERROR_INPUT_INVALID.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out:
        type: str
        indirection: 1
        kind: OUT
        description: Receives the payload as a UTF-8 string. The returned string is borrowed and valid until the value handle is destroyed.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_get_blob:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Returns the value as a byte string: a BLOB, or the storage bytes of a BIT
      or BIGNUM.

      The string is borrowed from the value and stays valid until the value is
      destroyed. That borrow is why this getter does not convert: a converted
      copy would not outlive the call. A value of any other type id, and a NULL
      value, return ERROR_INPUT_INVALID.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out:
        type: str
        indirection: 1
        kind: OUT
        description: Receives the payload as a byte string. The returned string is borrowed and valid until the value handle is destroyed.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_get_float:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Returns the value as a FLOAT.

      A value of a different type is converted through the default cast set, on
      a copy, so reading never alters the value. An unsupported conversion
      returns ERROR_INPUT_INVALID, as does a NULL value.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out:
        type: f32
        indirection: 1
        kind: OUT
        description: Receives the payload as a float.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_get_double:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Returns the value as a DOUBLE.

      A value of a different type is converted through the default cast set, on
      a copy, so reading never alters the value. An unsupported conversion
      returns ERROR_INPUT_INVALID, as does a NULL value.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out:
        type: f64
        indirection: 1
        kind: OUT
        description: Receives the payload as a double.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_get_date:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Returns the value as a DATE.

      The payload is days since 1970-01-01, the same unit a vector of
      this type exposes.

      A value of a different type is converted through the default cast set, on
      a copy, so reading never alters the value. An unsupported conversion
      returns ERROR_INPUT_INVALID, as does a NULL value.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out:
        type: i32
        indirection: 1
        kind: OUT
        description: Receives the payload.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_get_time:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Returns the value as a TIME.

      The payload is microseconds since midnight, the same unit a vector of
      this type exposes.

      A value of a different type is converted through the default cast set, on
      a copy, so reading never alters the value. An unsupported conversion
      returns ERROR_INPUT_INVALID, as does a NULL value.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out:
        type: i64
        indirection: 1
        kind: OUT
        description: Receives the payload.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_get_time_ns:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Returns the value as a TIME_NS.

      The payload is nanoseconds since midnight, the same unit a vector of
      this type exposes.

      A value of a different type is converted through the default cast set, on
      a copy, so reading never alters the value. An unsupported conversion
      returns ERROR_INPUT_INVALID, as does a NULL value.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out:
        type: i64
        indirection: 1
        kind: OUT
        description: Receives the payload.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_get_time_tz:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Returns the value as a TIME_TZ.

      The payload packs the time of day and the UTC offset into one integer, in
      the committed layout: micros in the high 40 bits, a biased offset in the
      low 24. The same form a vector of this type exposes.

      A value of a different type is converted through the default cast set, on
      a copy, so reading never alters the value. An unsupported conversion
      returns ERROR_INPUT_INVALID, as does a NULL value.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out:
        type: u64
        indirection: 1
        kind: OUT
        description: Receives the payload.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_get_timestamp:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Returns the value as a TIMESTAMP.

      The payload is microseconds since 1970-01-01, the same unit a vector of
      this type exposes.

      A value of a different type is converted through the default cast set, on
      a copy, so reading never alters the value. An unsupported conversion
      returns ERROR_INPUT_INVALID, as does a NULL value.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out:
        type: i64
        indirection: 1
        kind: OUT
        description: Receives the payload.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_get_timestamp_sec:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Returns the value as a TIMESTAMP_SEC.

      The payload is seconds since 1970-01-01, the same unit a vector of
      this type exposes.

      A value of a different type is converted through the default cast set, on
      a copy, so reading never alters the value. An unsupported conversion
      returns ERROR_INPUT_INVALID, as does a NULL value.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out:
        type: i64
        indirection: 1
        kind: OUT
        description: Receives the payload.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_get_timestamp_ms:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Returns the value as a TIMESTAMP_MS.

      The payload is milliseconds since 1970-01-01, the same unit a vector of
      this type exposes.

      A value of a different type is converted through the default cast set, on
      a copy, so reading never alters the value. An unsupported conversion
      returns ERROR_INPUT_INVALID, as does a NULL value.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out:
        type: i64
        indirection: 1
        kind: OUT
        description: Receives the payload.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_get_timestamp_ns:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Returns the value as a TIMESTAMP_NS.

      The payload is nanoseconds since 1970-01-01, the same unit a vector of
      this type exposes.

      A value of a different type is converted through the default cast set, on
      a copy, so reading never alters the value. An unsupported conversion
      returns ERROR_INPUT_INVALID, as does a NULL value.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out:
        type: i64
        indirection: 1
        kind: OUT
        description: Receives the payload.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_get_timestamp_tz:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Returns the value as a TIMESTAMP_TZ.

      The payload is microseconds since 1970-01-01, in UTC, the same unit a vector of
      this type exposes.

      A value of a different type is converted through the default cast set, on
      a copy, so reading never alters the value. An unsupported conversion
      returns ERROR_INPUT_INVALID, as does a NULL value.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out:
        type: i64
        indirection: 1
        kind: OUT
        description: Receives the payload.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_get_timestamp_tz_ns:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Returns the value as a TIMESTAMP_TZ_NS.

      The payload is nanoseconds since 1970-01-01, in UTC, the same unit a vector of
      this type exposes.

      A value of a different type is converted through the default cast set, on
      a copy, so reading never alters the value. An unsupported conversion
      returns ERROR_INPUT_INVALID, as does a NULL value.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out:
        type: i64
        indirection: 1
        kind: OUT
        description: Receives the payload.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_get_interval:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Returns the value as an INTERVAL.

      The payload is the (months, days, micros) triple, the same unit a vector of
      this type exposes.

      A value of a different type is converted through the default cast set, on
      a copy, so reading never alters the value. An unsupported conversion
      returns ERROR_INPUT_INVALID, as does a NULL value.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out:
        type: interval_t
        indirection: 1
        kind: OUT
        description: Receives the payload.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_get_uuid:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Returns the value as a UUID, in its internal 128-bit form.

      The dual of value_create_uuid: the storage form a vector element holds,
      with the high bit flipped so the integer sorts, rather than the canonical
      byte order. A value of any other type id, and a NULL value, return
      ERROR_INPUT_INVALID.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out:
        type: hugeint_t
        indirection: 1
        kind: OUT
        description: Receives the internal 128-bit storage form.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_get_decimal:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Returns the value as a DECIMAL: its backing integer plus the width and
      scale of its type.

      The integer is the value scaled by 10^scale — the dual of
      value_create_decimal — widened to 128 bits from whatever storage tier the
      width selects. The integer getters convert, so a DECIMAL read through
      value_get_bigint is its numeric value with the fraction dropped; this one
      always reports storage instead, together with the scale needed to
      interpret it. A value of any other type id, and a NULL value, return
      ERROR_INPUT_INVALID.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out:
        type: hugeint_t
        indirection: 1
        kind: OUT
        description: Receives the backing integer, scaled by 10^scale.
      out_width:
        type: u8
        indirection: 1
        kind: OUT
        description: Receives the total digit count of the value's type.
      out_scale:
        type: u8
        indirection: 1
        kind: OUT
        description: Receives the digits after the decimal point.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_get_type:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Unwraps the logical type carried by a TYPE value.

      Returns ERROR_INPUT_INVALID unless the value is a non-NULL TYPE value.
      The returned logical type is caller-owned and must be destroyed via
      logical_type_destroy.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out_type:
        type: logical_type
        indirection: 1
        kind: OUT
        description: Receives the owned logical type.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  # --------------------------------------------------------------------------------------------------------------------
  # Constructors (Context)
  # --------------------------------------------------------------------------------------------------------------------
  value_create_null_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a NULL value of the given logical type.

      The logical type is borrowed and copied into the value, so the caller can
      destroy it independently.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      type:
        type: logical_type
        description: The borrowed logical type to attach to the NULL value.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new NULL value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_bool_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a BOOLEAN value from a bool.

      The input is copied in. The returned value is caller-owned; destroy it
      via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      in_value:
        type: bool
        description: The bool to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new BOOLEAN value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_utinyint_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a UTINYINT value from a uint8_t.

      The input is copied in. The returned value is caller-owned; destroy it
      via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      in_value:
        type: u8
        description: The uint8_t to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new UTINYINT value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_usmallint_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a USMALLINT value from a uint16_t.

      The input is copied in. The returned value is caller-owned; destroy it
      via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      in_value:
        type: u16
        description: The uint16_t to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new USMALLINT value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_uint_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a UINTEGER value from a uint32_t.

      The input is copied in. The returned value is caller-owned; destroy it
      via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      in_value:
        type: u32
        description: The uint32_t to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new UINTEGER value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_ubigint_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a UBIGINT value from a uint64_t.

      The input is copied in. The returned value is caller-owned; destroy it
      via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      in_value:
        type: u64
        description: The uint64_t to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new UBIGINT value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_uhugeint_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a UHUGEINT value from a uint128_t.

      The input is copied in. The returned value is caller-owned; destroy it
      via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      in_value:
        type: uhugeint_t
        description: The uint128_t to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new UHUGEINT value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_tinyint_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a TINYINT value from an int8_t.

      The input is copied in. The returned value is caller-owned; destroy it
      via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      in_value:
        type: i8
        description: The int8_t to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new TINYINT value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_smallint_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a SMALLINT value from an int16_t.

      The input is copied in. The returned value is caller-owned; destroy it
      via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      in_value:
        type: i16
        description: The int16_t to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new SMALLINT value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_int_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates an INTEGER value from an int32_t.

      The input is copied in. The returned value is caller-owned; destroy it
      via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      in_value:
        type: i32
        description: The int32_t to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new INTEGER value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_bigint_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a BIGINT value from an int64_t.

      The input is copied in. The returned value is caller-owned; destroy it
      via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      in_value:
        type: i64
        description: The int64_t to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new BIGINT value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_hugeint_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a HUGEINT value from an int128_t.

      The input is copied in. The returned value is caller-owned; destroy it
      via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      in_value:
        type: hugeint_t
        description: The int128_t to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new HUGEINT value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_varchar_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a VARCHAR value from a UTF-8 string.

      The input is copied in. The returned value is caller-owned; destroy it
      via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      in_value:
        type: str
        description: The UTF-8 string to wrap. May be null only when len is 0 (empty string).
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new VARCHAR value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_blob_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a BLOB value from a byte string.

      The input is copied in. The returned value is caller-owned; destroy it
      via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      in_value:
        type: str
        description: The byte string to wrap. May be null only when len is 0 (empty string).
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new BLOB value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_float_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a FLOAT value from a float.

      The input is copied in. The returned value is caller-owned; destroy it
      via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      in_value:
        type: f32
        description: The float to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new FLOAT value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_double_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a DOUBLE value from a double.

      The input is copied in. The returned value is caller-owned; destroy it
      via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      in_value:
        type: f64
        description: The double to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new DOUBLE value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_type_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a TYPE value from a logical type.

      The logical type is borrowed and copied into the value, so the caller can
      destroy it independently.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      in_type:
        type: logical_type
        description: The borrowed logical type to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new TYPE value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  # --------------------------------------------------------------------------------------------------------------------
  # Constructors (Connection)
  # --------------------------------------------------------------------------------------------------------------------
  value_create_null_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a NULL value of the given logical type.

      The logical type is borrowed and copied into the value, so the caller can
      destroy it independently.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      type:
        type: logical_type
        description: The borrowed logical type to attach to the NULL value.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new NULL value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_bool_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a BOOLEAN value from a bool.

      The input is copied in. The returned value is caller-owned; destroy it
      via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      in_value:
        type: bool
        description: The bool to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new BOOLEAN value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_utinyint_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a UTINYINT value from a uint8_t.

      The input is copied in. The returned value is caller-owned; destroy it
      via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      in_value:
        type: u8
        description: The uint8_t to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new UTINYINT value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_usmallint_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a USMALLINT value from a uint16_t.

      The input is copied in. The returned value is caller-owned; destroy it
      via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      in_value:
        type: u16
        description: The uint16_t to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new USMALLINT value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_uint_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a UINTEGER value from a uint32_t.

      The input is copied in. The returned value is caller-owned; destroy it
      via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      in_value:
        type: u32
        description: The uint32_t to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new UINTEGER value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_ubigint_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a UBIGINT value from a uint64_t.

      The input is copied in. The returned value is caller-owned; destroy it
      via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      in_value:
        type: u64
        description: The uint64_t to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new UBIGINT value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_uhugeint_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a UHUGEINT value from a uint128_t.

      The input is copied in. The returned value is caller-owned; destroy it
      via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      in_value:
        type: uhugeint_t
        description: The uint128_t to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new UHUGEINT value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_tinyint_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a TINYINT value from an int8_t.

      The input is copied in. The returned value is caller-owned; destroy it
      via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      in_value:
        type: i8
        description: The int8_t to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new TINYINT value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_smallint_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a SMALLINT value from an int16_t.

      The input is copied in. The returned value is caller-owned; destroy it
      via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      in_value:
        type: i16
        description: The int16_t to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new SMALLINT value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_int_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates an INTEGER value from an int32_t.

      The input is copied in. The returned value is caller-owned; destroy it
      via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      in_value:
        type: i32
        description: The int32_t to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new INTEGER value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_bigint_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a BIGINT value from an int64_t.

      The input is copied in. The returned value is caller-owned; destroy it
      via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      in_value:
        type: i64
        description: The int64_t to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new BIGINT value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_hugeint_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a HUGEINT value from an int128_t.

      The input is copied in. The returned value is caller-owned; destroy it
      via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      in_value:
        type: hugeint_t
        description: The int128_t to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new HUGEINT value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_varchar_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a VARCHAR value from a UTF-8 string.

      The input is copied in. The returned value is caller-owned; destroy it
      via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      in_value:
        type: str
        description: The UTF-8 string to wrap. May be null only when len is 0 (empty string).
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new VARCHAR value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_blob_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a BLOB value from a byte string.

      The input is copied in. The returned value is caller-owned; destroy it
      via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      in_value:
        type: str
        description: The byte string to wrap. May be null only when len is 0 (empty string).
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new BLOB value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_float_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a FLOAT value from a float.

      The input is copied in. The returned value is caller-owned; destroy it
      via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      in_value:
        type: f32
        description: The float to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new FLOAT value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_double_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a DOUBLE value from a double.

      The input is copied in. The returned value is caller-owned; destroy it
      via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      in_value:
        type: f64
        description: The double to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new DOUBLE value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_type_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a TYPE value wrapping the given logical type.

      The input logical type is borrowed; the value internally copies the
      type so the caller can destroy the logical type independently.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      type:
        type: logical_type
        description: The borrowed logical type to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  # -----------------------------------------------------------------------
  # Composite construction + descent + cast
  # -----------------------------------------------------------------------
  value_create_date_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a DATE value.

      The payload is days since 1970-01-01, the same unit value_get_date reports.
      It is not range-checked here; value_cast is the validating path. The
      returned value is caller-owned; destroy it via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      in_value:
        type: i32
        description: The payload to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new DATE value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_date_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a DATE value.

      The payload is days since 1970-01-01, the same unit value_get_date reports.
      It is not range-checked here; value_cast is the validating path. The
      returned value is caller-owned; destroy it via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      in_value:
        type: i32
        description: The payload to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new DATE value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_time_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a TIME value.

      The payload is microseconds since midnight, the same unit value_get_time reports.
      It is not range-checked here; value_cast is the validating path. The
      returned value is caller-owned; destroy it via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      in_value:
        type: i64
        description: The payload to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new TIME value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_time_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a TIME value.

      The payload is microseconds since midnight, the same unit value_get_time reports.
      It is not range-checked here; value_cast is the validating path. The
      returned value is caller-owned; destroy it via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      in_value:
        type: i64
        description: The payload to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new TIME value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_time_ns_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a TIME_NS value.

      The payload is nanoseconds since midnight, the same unit value_get_time_ns reports.
      It is not range-checked here; value_cast is the validating path. The
      returned value is caller-owned; destroy it via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      in_value:
        type: i64
        description: The payload to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new TIME_NS value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_time_ns_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a TIME_NS value.

      The payload is nanoseconds since midnight, the same unit value_get_time_ns reports.
      It is not range-checked here; value_cast is the validating path. The
      returned value is caller-owned; destroy it via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      in_value:
        type: i64
        description: The payload to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new TIME_NS value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_time_tz_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a TIME_TZ value.

      The payload packs the time of day and the UTC offset into one integer, in
      the committed layout: micros in the high 40 bits, a biased offset in the
      low 24. The same form value_get_time_tz reports.
      It is not range-checked here; value_cast is the validating path. The
      returned value is caller-owned; destroy it via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      in_value:
        type: u64
        description: The payload to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new TIME_TZ value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_time_tz_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a TIME_TZ value.

      The payload packs the time of day and the UTC offset into one integer, in
      the committed layout: micros in the high 40 bits, a biased offset in the
      low 24. The same form value_get_time_tz reports.
      It is not range-checked here; value_cast is the validating path. The
      returned value is caller-owned; destroy it via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      in_value:
        type: u64
        description: The payload to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new TIME_TZ value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_timestamp_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a TIMESTAMP value.

      The payload is microseconds since 1970-01-01, the same unit value_get_timestamp reports.
      It is not range-checked here; value_cast is the validating path. The
      returned value is caller-owned; destroy it via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      in_value:
        type: i64
        description: The payload to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new TIMESTAMP value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_timestamp_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a TIMESTAMP value.

      The payload is microseconds since 1970-01-01, the same unit value_get_timestamp reports.
      It is not range-checked here; value_cast is the validating path. The
      returned value is caller-owned; destroy it via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      in_value:
        type: i64
        description: The payload to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new TIMESTAMP value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_timestamp_sec_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a TIMESTAMP_SEC value.

      The payload is seconds since 1970-01-01, the same unit value_get_timestamp_sec reports.
      It is not range-checked here; value_cast is the validating path. The
      returned value is caller-owned; destroy it via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      in_value:
        type: i64
        description: The payload to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new TIMESTAMP_SEC value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_timestamp_sec_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a TIMESTAMP_SEC value.

      The payload is seconds since 1970-01-01, the same unit value_get_timestamp_sec reports.
      It is not range-checked here; value_cast is the validating path. The
      returned value is caller-owned; destroy it via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      in_value:
        type: i64
        description: The payload to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new TIMESTAMP_SEC value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_timestamp_ms_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a TIMESTAMP_MS value.

      The payload is milliseconds since 1970-01-01, the same unit value_get_timestamp_ms reports.
      It is not range-checked here; value_cast is the validating path. The
      returned value is caller-owned; destroy it via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      in_value:
        type: i64
        description: The payload to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new TIMESTAMP_MS value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_timestamp_ms_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a TIMESTAMP_MS value.

      The payload is milliseconds since 1970-01-01, the same unit value_get_timestamp_ms reports.
      It is not range-checked here; value_cast is the validating path. The
      returned value is caller-owned; destroy it via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      in_value:
        type: i64
        description: The payload to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new TIMESTAMP_MS value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_timestamp_ns_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a TIMESTAMP_NS value.

      The payload is nanoseconds since 1970-01-01, the same unit value_get_timestamp_ns reports.
      It is not range-checked here; value_cast is the validating path. The
      returned value is caller-owned; destroy it via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      in_value:
        type: i64
        description: The payload to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new TIMESTAMP_NS value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_timestamp_ns_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a TIMESTAMP_NS value.

      The payload is nanoseconds since 1970-01-01, the same unit value_get_timestamp_ns reports.
      It is not range-checked here; value_cast is the validating path. The
      returned value is caller-owned; destroy it via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      in_value:
        type: i64
        description: The payload to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new TIMESTAMP_NS value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_timestamp_tz_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a TIMESTAMP_TZ value.

      The payload is microseconds since 1970-01-01, in UTC, the same unit
      value_get_timestamp_tz reports.
      It is not range-checked here; value_cast is the validating path. The
      returned value is caller-owned; destroy it via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      in_value:
        type: i64
        description: The payload to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new TIMESTAMP_TZ value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_timestamp_tz_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a TIMESTAMP_TZ value.

      The payload is microseconds since 1970-01-01, in UTC, the same unit
      value_get_timestamp_tz reports.
      It is not range-checked here; value_cast is the validating path. The
      returned value is caller-owned; destroy it via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      in_value:
        type: i64
        description: The payload to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new TIMESTAMP_TZ value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_timestamp_tz_ns_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a TIMESTAMP_TZ_NS value.

      The payload is nanoseconds since 1970-01-01, in UTC, the same unit
      value_get_timestamp_tz_ns reports.
      It is not range-checked here; value_cast is the validating path. The
      returned value is caller-owned; destroy it via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      in_value:
        type: i64
        description: The payload to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new TIMESTAMP_TZ_NS value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_timestamp_tz_ns_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a TIMESTAMP_TZ_NS value.

      The payload is nanoseconds since 1970-01-01, in UTC, the same unit
      value_get_timestamp_tz_ns reports.
      It is not range-checked here; value_cast is the validating path. The
      returned value is caller-owned; destroy it via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      in_value:
        type: i64
        description: The payload to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new TIMESTAMP_TZ_NS value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_interval_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a INTERVAL value.

      The payload is the (months, days, micros) triple, the same unit value_get_interval reports.
      It is not range-checked here; value_cast is the validating path. The
      returned value is caller-owned; destroy it via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      in_value:
        type: interval_t
        description: The payload to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new INTERVAL value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_interval_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a INTERVAL value.

      The payload is the (months, days, micros) triple, the same unit value_get_interval reports.
      It is not range-checked here; value_cast is the validating path. The
      returned value is caller-owned; destroy it via value_destroy.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      in_value:
        type: interval_t
        description: The payload to wrap.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new INTERVAL value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_decimal_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a DECIMAL value from its backing integer.

      The value is scaled by 10^scale, so (18500, 18, 3) is 18.500. width is
      the total digit count and must be 1..38; scale is the number of digits
      after the point and must not exceed width. Violating either returns
      ERROR_INPUT_INVALID, as does a value too wide for the storage tier the
      width selects. The value itself is not range-checked against the width;
      value_cast is the validating path.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      in_value:
        type: hugeint_t
        description: The backing integer, scaled by 10^scale.
      width:
        type: u8
        description: Total digit count, 1..38.
      scale:
        type: u8
        description: Digits after the decimal point; must not exceed width.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new DECIMAL value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_decimal_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a DECIMAL value from its backing integer.

      The value is scaled by 10^scale, so (18500, 18, 3) is 18.500. width is
      the total digit count and must be 1..38; scale is the number of digits
      after the point and must not exceed width. Violating either returns
      ERROR_INPUT_INVALID, as does a value too wide for the storage tier the
      width selects. The value itself is not range-checked against the width;
      value_cast is the validating path.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      in_value:
        type: hugeint_t
        description: The backing integer, scaled by 10^scale.
      width:
        type: u8
        description: Total digit count, 1..38.
      scale:
        type: u8
        description: Digits after the decimal point; must not exceed width.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new DECIMAL value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_uuid_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a UUID value from its internal 128-bit form.

      The payload is the storage form a vector element holds, not the canonical
      byte order: the high bit is flipped so the integer sorts. To build one
      from canonical text, cast a VARCHAR with value_cast instead, which is
      also how a UUID renders back.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      in_value:
        type: hugeint_t
        description: The internal 128-bit storage form.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new UUID value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_uuid_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a UUID value from its internal 128-bit form.

      The payload is the storage form a vector element holds, not the canonical
      byte order: the high bit is flipped so the integer sorts. To build one
      from canonical text, cast a VARCHAR with value_cast instead, which is
      also how a UUID renders back.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      in_value:
        type: hugeint_t
        description: The internal 128-bit storage form.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new UUID value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_bit_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a BIT value from its wire bytes.

      The wire form is a mandatory padding-header byte — the count of leading
      bits in the first data byte that are not part of the bit string —
      followed by the data bytes, so the input must be at least 1 byte. These
      are the same bytes value_get_blob reports back.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      in_value:
        type: str
        description: The wire bytes, a padding header byte followed by data. Must be at least 1 byte.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new BIT value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_bit_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a BIT value from its wire bytes.

      The wire form is a mandatory padding-header byte — the count of leading
      bits in the first data byte that are not part of the bit string —
      followed by the data bytes, so the input must be at least 1 byte. These
      are the same bytes value_get_blob reports back.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      in_value:
        type: str
        description: The wire bytes, a padding header byte followed by data. Must be at least 1 byte.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new BIT value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_bignum_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a BIGNUM value from its storage bytes.

      The bytes are opaque storage, as produced by bignum_encode from a
      magnitude and a sign flag; a negative is stored bit-inverted behind a
      header, so these are not the magnitude bytes. The header plus at least
      one magnitude byte means the input must exceed 3 bytes. These are the
      same bytes value_get_blob reports back, for bignum_decode to translate.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      in_value:
        type: str
        description: The opaque storage bytes from bignum_encode. Must exceed 3 bytes.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new BIGNUM value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  value_create_bignum_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a BIGNUM value from its storage bytes.

      The bytes are opaque storage, as produced by bignum_encode from a
      magnitude and a sign flag; a negative is stored bit-inverted behind a
      header, so these are not the magnitude bytes. The header plus at least
      one magnitude byte means the input must exceed 3 bytes. These are the
      same bytes value_get_blob reports back, for bignum_decode to translate.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      in_value:
        type: str
        description: The opaque storage bytes from bignum_encode. Must exceed 3 bytes.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new BIGNUM value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR

  # -----------------------------------------------------------------------
  # Composite constructors
  # -----------------------------------------------------------------------
  value_create_list_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a LIST value from its elements.

      The child type is the common type of the elements, resolved by the same
      rule a SQL list literal follows, and every element is cast to it; a set
      with no common type surfaces the cast error. Pass child_type to name the
      type instead, which is also how an empty list is built: with child_type
      NULL, an empty element array has no type to resolve and returns
      ERROR_INPUT_INVALID.

      The elements are borrowed and copied in, and a NULL element becomes a
      typed NULL. The type is rebuilt from its child, so an alias on the outer
      LIST type is not preserved; value_cast is the alias-preserving path.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      child_type:
        type: logical_type
        description: Optional. The element type. Pass NULL to resolve it from the elements.
      children:
        type: value
        indirection: 1
        const: true
        description: An array of child_count elements. Borrowed (copied in). Pass NULL when child_count is 0.
      child_count:
        type: idx
        description: The number of elements.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new composite value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR
  value_create_list_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a LIST value from its elements.

      The child type is the common type of the elements, resolved by the same
      rule a SQL list literal follows, and every element is cast to it; a set
      with no common type surfaces the cast error. Pass child_type to name the
      type instead, which is also how an empty list is built: with child_type
      NULL, an empty element array has no type to resolve and returns
      ERROR_INPUT_INVALID.

      The elements are borrowed and copied in, and a NULL element becomes a
      typed NULL. The type is rebuilt from its child, so an alias on the outer
      LIST type is not preserved; value_cast is the alias-preserving path.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      child_type:
        type: logical_type
        description: Optional. The element type. Pass NULL to resolve it from the elements.
      children:
        type: value
        indirection: 1
        const: true
        description: An array of child_count elements. Borrowed (copied in). Pass NULL when child_count is 0.
      child_count:
        type: idx
        description: The number of elements.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new composite value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR
  value_create_array_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates an ARRAY value from its elements, sized by their count.

      The child type is resolved exactly as for value_create_list, and
      child_type names it explicitly the same way. The minimum array size is 1,
      so an empty element array returns ERROR_INPUT_INVALID whether or not
      child_type is given.

      The elements are borrowed and copied in, and a NULL element becomes a
      typed NULL.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      child_type:
        type: logical_type
        description: Optional. The element type. Pass NULL to resolve it from the elements.
      children:
        type: value
        indirection: 1
        const: true
        description: An array of child_count elements. Borrowed (copied in). Pass NULL when child_count is 0.
      child_count:
        type: idx
        description: The number of elements.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new composite value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR
  value_create_array_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates an ARRAY value from its elements, sized by their count.

      The child type is resolved exactly as for value_create_list, and
      child_type names it explicitly the same way. The minimum array size is 1,
      so an empty element array returns ERROR_INPUT_INVALID whether or not
      child_type is given.

      The elements are borrowed and copied in, and a NULL element becomes a
      typed NULL.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      child_type:
        type: logical_type
        description: Optional. The element type. Pass NULL to resolve it from the elements.
      children:
        type: value
        indirection: 1
        const: true
        description: An array of child_count elements. Borrowed (copied in). Pass NULL when child_count is 0.
      child_count:
        type: idx
        description: The number of elements.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new composite value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR
  value_create_struct_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a STRUCT value from named fields, in order.

      Each field keeps its own child's type, so there is nothing to resolve
      across them; names and children are parallel arrays of field_count
      entries. An empty field list builds the empty struct, which is a real
      type.

      The names and children are borrowed and copied in, and a NULL child
      becomes a typed NULL. Duplicate field names are rejected.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      names:
        type: identifier
        indirection: 1
        const: true
        description: An array of field_count field names. Pass NULL when field_count is 0.
      children:
        type: value
        indirection: 1
        const: true
        description: An array of field_count field values. Borrowed (copied in). Pass NULL when field_count is 0.
      field_count:
        type: idx
        description: The number of fields.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new composite value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR
  value_create_struct_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a STRUCT value from named fields, in order.

      Each field keeps its own child's type, so there is nothing to resolve
      across them; names and children are parallel arrays of field_count
      entries. An empty field list builds the empty struct, which is a real
      type.

      The names and children are borrowed and copied in, and a NULL child
      becomes a typed NULL. Duplicate field names are rejected.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      names:
        type: identifier
        indirection: 1
        const: true
        description: An array of field_count field names. Pass NULL when field_count is 0.
      children:
        type: value
        indirection: 1
        const: true
        description: An array of field_count field values. Borrowed (copied in). Pass NULL when field_count is 0.
      field_count:
        type: idx
        description: The number of fields.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new composite value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR
  value_create_tuple_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a TUPLE value from positional fields.

      TUPLE is the unnamed struct: the same positional children as
      value_create_struct, under a distinct type id and with no names. Each
      field keeps its own child's type. An empty field list builds the empty
      tuple, which is a real type.

      The children are borrowed and copied in, and a NULL child becomes a typed
      NULL.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      children:
        type: value
        indirection: 1
        const: true
        description: An array of field_count field values. Borrowed (copied in). Pass NULL when field_count is 0.
      field_count:
        type: idx
        description: The number of fields.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new composite value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR
  value_create_tuple_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a TUPLE value from positional fields.

      TUPLE is the unnamed struct: the same positional children as
      value_create_struct, under a distinct type id and with no names. Each
      field keeps its own child's type. An empty field list builds the empty
      tuple, which is a real type.

      The children are borrowed and copied in, and a NULL child becomes a typed
      NULL.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      children:
        type: value
        indirection: 1
        const: true
        description: An array of field_count field values. Borrowed (copied in). Pass NULL when field_count is 0.
      field_count:
        type: idx
        description: The number of fields.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new composite value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR
  value_create_map_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a MAP value from parallel key and value arrays.

      The key and value types are the common types of the keys and of the
      values, resolved as for value_create_list, and every entry is cast to
      them. Pass key_type and value_type to name them instead, which is also
      how an empty map is built: with both NULL, empty key and value arrays
      have no types to resolve and return ERROR_INPUT_INVALID.

      Keys and values are borrowed and copied in, and the two arrays must be
      the same length. Keys must be non-NULL and unique; both are enforced.
    role: constructor
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context to use for the construction.
      key_type:
        type: logical_type
        description: Optional. The key type. Pass NULL to resolve it from the keys.
      value_type:
        type: logical_type
        description: Optional. The value type. Pass NULL to resolve it from the values.
      keys:
        type: value
        indirection: 1
        const: true
        description: An array of entry_count keys. Borrowed (copied in). Pass NULL when entry_count is 0.
      values:
        type: value
        indirection: 1
        const: true
        description: An array of entry_count values, parallel to keys. Borrowed (copied in). Pass NULL when entry_count is 0.
      entry_count:
        type: idx
        description: The number of entries.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new composite value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR
  value_create_map_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Creates a MAP value from parallel key and value arrays.

      The key and value types are the common types of the keys and of the
      values, resolved as for value_create_list, and every entry is cast to
      them. Pass key_type and value_type to name them instead, which is also
      how an empty map is built: with both NULL, empty key and value arrays
      have no types to resolve and return ERROR_INPUT_INVALID.

      Keys and values are borrowed and copied in, and the two arrays must be
      the same length. Keys must be non-NULL and unique; both are enforced.
    role: constructor
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection to use for the construction.
      key_type:
        type: logical_type
        description: Optional. The key type. Pass NULL to resolve it from the keys.
      value_type:
        type: logical_type
        description: Optional. The value type. Pass NULL to resolve it from the values.
      keys:
        type: value
        indirection: 1
        const: true
        description: An array of entry_count keys. Borrowed (copied in). Pass NULL when entry_count is 0.
      values:
        type: value
        indirection: 1
        const: true
        description: An array of entry_count values, parallel to keys. Borrowed (copied in). Pass NULL when entry_count is 0.
      entry_count:
        type: idx
        description: The number of entries.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the new composite value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR
  value_get_child_count:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Returns the number of child values of a value.

      Per type kind: the element count for LIST and ARRAY; the field count for
      STRUCT and TUPLE; twice the entry count for MAP, whose children alternate
      key, value; and 2 for UNION, the tag plus the active member. Everything
      else reports 0, NULL values of any nested type included.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out_count:
        type: idx
        indirection: 1
        kind: OUT
        description: Receives the number of children.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR
  value_get_child:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Returns one child of a composite value as an owned copy.

      LIST and ARRAY children are the elements. STRUCT and TUPLE children are
      the fields in declared order, and a STRUCT's field names come from its
      type, via logical_type_get_param. MAP children alternate key, value,
      symmetric with value_create_map. UNION children are [0] the tag, as a
      UTINYINT value, and [1] the active member, because a union value holds
      only that one member.

      Note the divergence from vector_get_child, which descends structurally
      and so exposes [0] = tag and [1..N] = ALL members. Code written to
      descend generically over both must account for that difference.

      An out-of-range index returns ERROR_INPUT_INVALID, as does any index on a
      non-composite or NULL value. The returned value is caller-owned; destroy
      it via value_destroy.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      index:
        type: idx
        description: The child index, in [0, child_count).
      out_child:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the owned child value.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR
  value_cast_with_connection:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Casts a value to a target type, using a connection.

      The same as value_cast_with_context, except that the cast function set
      comes from a connection: the cast runs in its own transaction on that
      connection's context. Use it from outside DuckDB, where a connection —
      but no context — is in hand.

      The conversion is the SQL-faithful, non-strict one, registered custom
      casts included, and a cast failure surfaces from the call. Together with
      a VARCHAR built through value_create_varchar_with_context /
      _with_connection, this constructs any value from text, extension values
      included; casting a member value to a union type, or a VARCHAR to an enum
      type, is the sanctioned way to build UNION and ENUM values.

      The input value and target type are borrowed. The returned value is
      caller-owned; destroy it via value_destroy.
    role: method
    belongs_to: value
    parameters:
      conn:
        type: connection
        description: The connection whose context supplies the cast function set.
      value:
        type: value
        description: The borrowed value to cast.
      target_type:
        type: logical_type
        description: The borrowed target logical type.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the owned cast result.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR
  value_cast_with_context:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Casts a value to a target type, using a context.

      The conversion is the SQL-faithful, non-strict one, registered custom
      casts included, and a cast failure surfaces from the call. Together with
      a VARCHAR built through value_create_varchar_with_context /
      _with_connection, this constructs any value from text, extension values
      included; casting a member value to a union type, or a VARCHAR to an enum
      type, is the sanctioned way to build UNION and ENUM values.

      Runs in the caller's context scope, as create_type_from_text does: reach
      it from a bind-phase callback or another context-holding scope, not from
      an exec-phase worker callback. From outside DuckDB use
      value_cast_with_connection.

      The input value and target type are borrowed. The returned value is
      caller-owned; destroy it via value_destroy.
    role: method
    belongs_to: value
    parameters:
      ctx:
        type: context
        description: The context supplying the cast function set.
      value:
        type: value
        description: The borrowed value to cast.
      target_type:
        type: logical_type
        description: The borrowed target logical type.
      out_value:
        type: value
        indirection: 1
        kind: OUT
        description: Receives the owned cast result.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR
  # -----------------------------------------------------------------------
  # Common introspection
  # -----------------------------------------------------------------------
  value_is_null:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: Returns whether the value is NULL.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out_is_null:
        type: bool
        indirection: 1
        kind: OUT
        description: Receives true if the value is NULL, false otherwise.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR
  value_get_logical_type:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Returns the logical type of the value.

      The returned logical type is caller-owned; destroy it via
      logical_type_destroy.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out_type:
        type: logical_type
        indirection: 1
        kind: OUT
        description: Receives the owned logical type.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR
  value_to_string:
    lifecycle:
      - [ "stable", "v2.0.0", "2026-08-04" ]
    description: |
      Renders the value as a human-readable string. Diagnostic only.

      Writes into a caller-supplied buffer, so nothing is allocated on the
      caller's behalf and nothing has to be freed.

      Pass out_string = NULL to size the buffer without rendering into it:
      out_length then receives the length, and out_capacity is ignored. With
      out_string != NULL, out_capacity must be at least out_length + 1, or the
      call returns ERROR_INPUT_OBJECT_SIZE with out_length set to the required
      length and out_string left untouched.

      out_length never counts the terminator, but a successful write always
      appends one, so the buffer is usable as a C string.
    role: getter
    belongs_to: value
    parameters:
      value:
        type: value
        description: The value to read.
      out_string:
        type: char
        indirection: 1
        kind: OUT
        description: Caller-owned buffer receiving the text plus a null terminator, or NULL to only report the required length in out_length.
      out_capacity:
        type: idx
        description: Bytes available in out_string, terminator included. Ignored when out_string is NULL.
      out_length:
        type: idx
        indirection: 1
        kind: OUT
        description: Receives the text length excluding the null terminator — written on success and on ERROR_INPUT_OBJECT_SIZE.
      err:
        type: error_info
        indirection: 1
        kind: OUT
        description: Optional. On failure, receives an opaque info handle the caller must destroy via error_info_destroy.
    return_type: ERROR
