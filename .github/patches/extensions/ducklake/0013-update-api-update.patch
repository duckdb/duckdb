commit 86b967deef641cbca2561f66b77af37a08e0db58
Author: dentinyhao <dentinyhao@gmail.com>
Date:   Sat Sep 12 22:57:12 2026 -0700

    update API update

diff --git a/src/storage/ducklake_update.cpp b/src/storage/ducklake_update.cpp
index 95fe20d5..5e0d9c84 100644
--- a/src/storage/ducklake_update.cpp
+++ b/src/storage/ducklake_update.cpp
@@ -237,15 +237,15 @@ DuckLakeUpdate &DuckLakeUpdate::PlanUpdateOperator(ClientContext &context, Physi
 	// build update expressions (physical columns only, no partition cols, no casts)
 	vector<unique_ptr<Expression>> expressions;
 	unordered_map<idx_t, idx_t> expression_map;
-	for (idx_t i = 0; i < op.columns.size(); i++) {
-		expression_map[op.columns[i].index] = i;
+	for (idx_t i = 0; i < op.referenced_columns.size(); i++) {
+		expression_map[op.referenced_columns[i].index] = i;
 	}
-	for (idx_t i = 0; i < op.columns.size(); i++) {
+	for (idx_t i = 0; i < op.referenced_columns.size(); i++) {
 		expressions.push_back(op.expressions[expression_map[i]]->Copy());
 	}
 
-	auto &update_op =
-	    planner.Make<DuckLakeUpdate>(table, op.columns, child_plan, delete_op, expressions).Cast<DuckLakeUpdate>();
+	auto &update_op = planner.Make<DuckLakeUpdate>(table, op.referenced_columns, child_plan, delete_op, expressions)
+	                      .Cast<DuckLakeUpdate>();
 
 	// set output types we use physical column types + BIGINT row_id
 	vector<LogicalType> update_output_types;
@@ -299,7 +299,7 @@ void DuckLakeTableEntry::BindUpdateConstraints(Binder &binder, LogicalGet &get,
 	for (auto &column : columns.Physical()) {
 		auto physical_index = column.Physical();
 		bool found = false;
-		for (auto &col : update.columns) {
+		for (auto &col : update.referenced_columns) {
 			if (col == physical_index) {
 				found = true;
 				break;
@@ -328,7 +328,7 @@ void DuckLakeTableEntry::BindUpdateConstraints(Binder &binder, LogicalGet &get,
 		proj.expressions.push_back(make_uniq<BoundColumnRefExpression>(
 		    column.Type(), ColumnBinding(get.table_index, ProjectionIndex(column_id_index.GetIndex()))));
 		get.AddColumnId(physical_index.index);
-		update.columns.push_back(physical_index);
+		update.referenced_columns.push_back(physical_index);
 	}
 }
 
