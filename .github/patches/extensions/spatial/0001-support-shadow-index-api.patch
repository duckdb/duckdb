diff --git a/src/spatial/index/rtree/rtree.hpp b/src/spatial/index/rtree/rtree.hpp
index e35f303..5d9ab9f 100644
--- a/src/spatial/index/rtree/rtree.hpp
+++ b/src/spatial/index/rtree/rtree.hpp
@@ -31,8 +31,13 @@ public:
 	RTree(BlockManager &block_manager, const RTreeConfig &config_p) : config(config_p) {
 
 		// Create the allocators
-		node_allocator = make_uniq<FixedSizeAllocator>(config.GetNodeByteSize(), block_manager);
-		leaf_allocator = make_uniq<FixedSizeAllocator>(config.GetLeafByteSize(), block_manager);
+		node_allocator = make_unsafe_uniq<FixedSizeAllocator>(config.GetNodeByteSize(), block_manager);
+		leaf_allocator = make_unsafe_uniq<FixedSizeAllocator>(config.GetLeafByteSize(), block_manager);
+	}
+
+	RTree(const RTreeConfig &config_p, unsafe_unique_ptr<FixedSizeAllocator> leaf_allocator_p,
+	      unsafe_unique_ptr<FixedSizeAllocator> node_allocator_p)
+	    : node_allocator(std::move(node_allocator_p)), leaf_allocator(std::move(leaf_allocator_p)), config(config_p) {
 	}
 
 	void Insert(const RTreeEntry &entry) {
@@ -107,12 +112,12 @@ private:
 	void ReInsertNode(RTreeEntry &root, RTreeEntry &target);
 
 private:
-	unique_ptr<FixedSizeAllocator> node_allocator;
-	unique_ptr<FixedSizeAllocator> leaf_allocator;
+	unsafe_unique_ptr<FixedSizeAllocator> node_allocator;
+	unsafe_unique_ptr<FixedSizeAllocator> leaf_allocator;
 
 	RTreeEntry root;
 
 	const RTreeConfig config;
 };
 
-} // namespace duckdb
\ No newline at end of file
+} // namespace duckdb
diff --git a/src/spatial/index/rtree/rtree_index.cpp b/src/spatial/index/rtree/rtree_index.cpp
index f793b05..bfff3ed 100644
--- a/src/spatial/index/rtree/rtree_index.cpp
+++ b/src/spatial/index/rtree/rtree_index.cpp
@@ -8,6 +8,7 @@
 #include "duckdb/common/serializer/binary_deserializer.hpp"
 #include "duckdb/common/serializer/binary_serializer.hpp"
 #include "duckdb/execution/index/fixed_size_allocator.hpp"
+#include "duckdb/storage/checkpoint/table_index_writer.hpp"
 #include "duckdb/storage/table/scan_state.hpp"
 #include "duckdb/main/database.hpp"
 #include "spatial/spatial_types.hpp"
@@ -126,6 +127,29 @@ RTreeIndex::RTreeIndex(const Identifier &name, IndexConstraintType index_constra
 	key_chunk.Initialize(context, {GeoTypes::BOX_2DF()});
 }
 
+RTreeIndex::RTreeIndex(const RTreeIndex &src, const RTreeEntry &root,
+                       unsafe_unique_ptr<FixedSizeAllocator> leaf_allocator,
+                       unsafe_unique_ptr<FixedSizeAllocator> node_allocator)
+    : BoundIndex(Identifier(src.name), TYPE_NAME, src.index_constraint_type, src.column_ids, src.table_io_manager,
+                 src.unbound_expressions, src.db) {
+	const auto [max_node_capacity, min_node_capacity] = src.tree->GetConfig();
+	tree = make_uniq<RTree>(RTreeConfig {max_node_capacity, min_node_capacity}, std::move(leaf_allocator),
+	                        std::move(node_allocator));
+	tree->SetRoot(root);
+
+	auto &context = src.key_executor->GetContext();
+	key_expr = src.key_expr->Copy();
+	key_executor = make_uniq<ExpressionExecutor>(context);
+	key_executor->AddExpression(*key_expr);
+	key_chunk.Initialize(context, {GeoTypes::BOX_2DF()});
+}
+
+unique_ptr<BoundIndex> RTreeIndex::CreateShadow(const RTreeEntry &root,
+                                                unsafe_unique_ptr<FixedSizeAllocator> leaf_allocator,
+                                                unsafe_unique_ptr<FixedSizeAllocator> node_allocator) {
+	return make_uniq<RTreeIndex>(*this, root, std::move(leaf_allocator), std::move(node_allocator));
+}
+
 unique_ptr<IndexScanState> RTreeIndex::InitializeScan(const RTreeBounds &query) const {
 	auto state = make_uniq<RTreeIndexScanState>();
 	state->query_bounds = query;
@@ -234,32 +258,27 @@ void RTreeIndex::Delete(IndexLock &lock, DataChunk &input, Vector &row_vec) {
 	ConvertToEntries(box_vec, row_vec, count, [&](const RTreeEntry &entry) { tree->Delete(entry); });
 }
 
-IndexStorageInfo RTreeIndex::SerializeToDisk(QueryContext context, const case_insensitive_map_t<Value> &options) {
-
+CheckpointedIndex RTreeIndex::Checkpoint(PartialBlockManager &partial_block_manager, StorageVersion) {
+	lock_guard<mutex> guard(lock);
+	const auto root = tree->GetRoot();
 	IndexStorageInfo info;
 	info.name = name;
-	info.root = tree->GetRoot().pointer.Get();
-
+	info.root = root.pointer.Get();
 	auto &leaf_allocator = tree->GetLeafAllocator();
 	auto &node_allocator = tree->GetNodeAllocator();
 
-	leaf_allocator.RemoveEmptyBuffers();
-	node_allocator.RemoveEmptyBuffers();
-
-	// Use the partial block manager to serialize allocator data.
-	auto &block_manager = table_io_manager.GetIndexBlockManager();
-	PartialBlockManager partial_block_manager(context, block_manager, PartialBlockType::FULL_CHECKPOINT);
-	leaf_allocator.SerializeBuffers(partial_block_manager);
-	node_allocator.SerializeBuffers(partial_block_manager);
-	partial_block_manager.FlushPartialBlocks();
-
-	info.allocator_infos.push_back(leaf_allocator.GetInfo());
-	info.allocator_infos.push_back(node_allocator.GetInfo());
-
-	return info;
+	auto new_leaf_allocator = leaf_allocator.Persist(partial_block_manager);
+	auto new_node_allocator = node_allocator.Persist(partial_block_manager);
+	new_leaf_allocator->RemoveEmptyBuffers();
+	new_node_allocator->RemoveEmptyBuffers();
+	info.allocator_infos.push_back(new_leaf_allocator->GetInfo());
+	info.allocator_infos.push_back(new_node_allocator->GetInfo());
+	return {make_shared_ptr<IndexStorageInfo>(std::move(info)),
+	        CreateShadow(root, std::move(new_leaf_allocator), std::move(new_node_allocator))};
 }
 
-IndexStorageInfo RTreeIndex::SerializeToWAL(const case_insensitive_map_t<Value> &options) {
+IndexStorageInfo RTreeIndex::SerializeToWAL(StorageVersion target_version) {
+	lock_guard<mutex> guard(lock);
 
 	IndexStorageInfo info;
 	info.name = name;
diff --git a/src/spatial/index/rtree/rtree_index.hpp b/src/spatial/index/rtree/rtree_index.hpp
index 0f51959..f4ce858 100644
--- a/src/spatial/index/rtree/rtree_index.hpp
+++ b/src/spatial/index/rtree/rtree_index.hpp
@@ -23,6 +23,12 @@ public:
 	           TableIOManager &table_io_manager, const vector<unique_ptr<Expression>> &unbound_expressions,
 	           AttachedDatabase &db, const case_insensitive_map_t<Value> &options, ClientContext &context,
 	           const IndexStorageInfo &info = IndexStorageInfo(), idx_t estimated_cardinality = 0);
+	RTreeIndex(const RTreeIndex &src, const RTreeEntry &root,
+	           unsafe_unique_ptr<FixedSizeAllocator> leaf_allocator,
+	           unsafe_unique_ptr<FixedSizeAllocator> node_allocator);
+	unique_ptr<BoundIndex> CreateShadow(const RTreeEntry &root,
+	                                   unsafe_unique_ptr<FixedSizeAllocator> leaf_allocator,
+	                                   unsafe_unique_ptr<FixedSizeAllocator> node_allocator);
 
 	unique_ptr<RTree> tree;
 
@@ -55,9 +61,9 @@ public:
 	ErrorData Insert(IndexLock &lock, DataChunk &data, Vector &row_ids) override;
 
 	//! Serializes RTree memory to disk and returns the index storage information.
-	IndexStorageInfo SerializeToDisk(QueryContext context, const case_insensitive_map_t<Value> &options) override;
+	CheckpointedIndex Checkpoint(PartialBlockManager &partial_block_manager, StorageVersion) override;
 	//! Serializes RTree memory to the WAL and returns the index storage information.
-	IndexStorageInfo SerializeToWAL(const case_insensitive_map_t<Value> &options) override;
+	IndexStorageInfo SerializeToWAL(StorageVersion target_version) override;
 
 	idx_t GetInMemorySize(IndexLock &state) override;
 

