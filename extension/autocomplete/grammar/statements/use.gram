UseStatement[unique_ptr<SQLStatement>] <- 'USE' UseTarget

UseTarget[QualifiedName] <- UseTargetCatalogSchema / SchemaName / CatalogName
UseTargetCatalogSchema[QualifiedName] <- CatalogName '.' ReservedSchemaName ('.' Identifier)*
