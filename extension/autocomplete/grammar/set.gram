SetStatement <- 'SET'i (SetSetting / SetVariable)

SetSetting <- SettingScope? SettingName SetAssignment
SetVariable <- 'VARIABLE'i Identifier SetAssignment

SettingScope <- 'LOCAL'i / 'SESSION'i / 'GLOBAL'i

SetAssignment <- VariableAssign VariableList

VariableAssign <- '=' / 'TO'
VariableList <- List(Expression)
