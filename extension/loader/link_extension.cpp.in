// Generated by CMake from extension/loader/link_extension.cpp.in. Do not edit.
//
// Roots the @NAME@ extension into a binary. Nothing references this object, so a link names it with
// -u duckdb_extension_@NAME@_root (see duckdb_link_root in extension_build_tools.cmake); that extracts it from the
// extension's archive, and its registrar then publishes the extension to the engine before main.
// A loadable extension never names it, so this object stays out of it. Clients that include
// duckdb_autolink.h name every known extension, so putting the archive on the link line is enough.

#include "duckdb/main/database.hpp"
#include "duckdb/main/extension/linked_extension_registry.hpp"
@LINK_INCLUDES@
@LINK_DECLARATIONS@
extern "C" DUCKDB_EXTENSION_API void duckdb_extension_@NAME@_root() {
}

namespace duckdb {

static LinkedExtensionRegistrar @NAME@_registrar("@NAME@", [](DuckDB &db) {
	@LINK_BODY@
});

} // namespace duckdb
