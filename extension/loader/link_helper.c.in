// Generated from extension/loader/link_helper.c.in. Do not edit.
// Links the extensions: @LINK_EXTENSION_LIST@
//
// Each extension archive carries a root member, duckdb_extension_<name>_root, that registers the extension with the
// engine. Nothing references it, so this file does: compile it into the program and put the extension archives before
// libduckdb_static.a. A listed extension whose archive is missing fails the link.

#if defined(_MSC_VER)
// an /include directive is how the MSVC linker is told to want a symbol nothing references
#if defined(_M_IX86)
@MSVC_X86_INCLUDES@
#else
@MSVC_INCLUDES@
#endif
// keeps the translation unit from being empty
typedef int duckdb_link_helper_unused;
#else
#ifdef __cplusplus
extern "C" {
#endif
@ROOT_DECLARATIONS@
#ifdef __cplusplus
}
#endif

// referencing the roots from an always-linked object pulls their members out of the extension archives
__attribute__((used)) static void (*const duckdb_extension_roots[])(void) = {
@ROOT_TABLE@
};
#endif
