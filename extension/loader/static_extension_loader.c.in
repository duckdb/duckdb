// Generated from extension/loader/static_extension_loader.c.in. Do not edit.
// Links the extensions: @LINK_EXTENSION_LIST@
//
// Static extension loader: references each extension's root member so the linker pulls it in.
// Compile it into the program and put the extension archives before libduckdb_static.a.

#if defined(_MSC_VER)
// an /include directive is how the MSVC linker is told to want a symbol nothing references
#if defined(_M_IX86)
@MSVC_X86_INCLUDES@
#else
@MSVC_INCLUDES@
#endif
// keeps the translation unit from being empty
typedef int duckdb_static_extension_loader_unused;
#else
#ifdef __cplusplus
extern "C" {
#endif
@ROOT_DECLARATIONS@
#ifdef __cplusplus
}
#endif

// referencing the roots from an always-linked object pulls their members out of the extension archives
__attribute__((used)) static void (*const duckdb_extension_roots[])(void) = {
@ROOT_TABLE@
};
#endif
