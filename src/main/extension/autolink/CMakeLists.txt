# Generated by scripts/generate_autolink.py from extension/known_extensions.txt.
# Do not edit. One object per name, never a unity build: the engine archive must
# be able to supply exactly the names whose extension archive is absent from a
# link.
add_library(
  duckdb_autolink_fallbacks OBJECT
  autocomplete_root_fallback.cpp
  avro_root_fallback.cpp
  aws_root_fallback.cpp
  azure_root_fallback.cpp
  core_functions_root_fallback.cpp
  delta_root_fallback.cpp
  ducklake_root_fallback.cpp
  encodings_root_fallback.cpp
  excel_root_fallback.cpp
  fts_root_fallback.cpp
  httpfs_root_fallback.cpp
  iceberg_root_fallback.cpp
  icu_root_fallback.cpp
  inet_root_fallback.cpp
  json_root_fallback.cpp
  lance_root_fallback.cpp
  motherduck_root_fallback.cpp
  mysql_scanner_root_fallback.cpp
  odbc_scanner_root_fallback.cpp
  parquet_root_fallback.cpp
  postgres_scanner_root_fallback.cpp
  quack_root_fallback.cpp
  spatial_root_fallback.cpp
  sqlite_scanner_root_fallback.cpp
  tpcds_root_fallback.cpp
  tpch_root_fallback.cpp
  ui_root_fallback.cpp
  unity_catalog_root_fallback.cpp
  vortex_root_fallback.cpp
  vss_root_fallback.cpp)

# The same objects as an archive, for links that need them after the extension
# archives rather than as always-included objects (the shared library exports
# every known name this way).
add_library(duckdb_autolink_fallbacks_archive STATIC
            $<TARGET_OBJECTS:duckdb_autolink_fallbacks>)
